// Cuando haces ng build --prod, Angular reemplaza environment.ts con environment.prod.ts.

export const environment = {
  production: true,
  apiUrl: 'https://devicecontrolapi-latest.onrender.com'
};
